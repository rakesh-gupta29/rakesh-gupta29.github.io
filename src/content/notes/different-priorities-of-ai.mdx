---
title: "How AI has diffent priorities and we should be worried"
slug: "different-priorities-of-ai"
description: "Late night rant about how AI does not give a f*ck about you"
tags: ["tooling", "rant"]
pubDate: "Oct 3, 2025"
---

import Image from "../../components/Image.astro";

Normal day of using Cursor to write my site at 2AM and got a relevation

## The background

Backend will be sending some permissions in json format. parse that, and load the modules for user.

Simple logic to pass each module through a permission check function and get a boolean flag for whether to show it or not.

but the ai has different priorities 

the whole point of a high level programming language is to have human readable code and abstractions. 
otherwise why not make claude write something gibberish which does works

Also, LLMs are 'language' models. If code is better documented or has naming that is intuitive that makes sense, even other LLMs would understand it better. Even if they can parse the code faster than any human, there is a lot of information conveyed by naming and comments that would simply be missing

token efficiency over the human readibility

Labs do these tests even way better. I wonder what all they have seen which we don't because they never published.

the gibberish code does not mean that it is efficient or works but have no way to verify that it works

Yeah it's unreadable by a human. Regardless, one can submit it again to an AI and ask it to explain.

LLMs understand yes, and if you have trained a model from scratch then you’ll literally see that they choose what to compress from dumped data in training. They even choose what to choose on inference.

https://www.youtube.com/watch?v=EtNagNezo8w

https://appinventiv.com/blog/fb-shuts-artificial-intelligence-system/

https://ibb.co/67V2ByKR

